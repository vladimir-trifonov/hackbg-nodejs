{
  "name": "1-Chat-Server-And-Clients",
  "version": "1.0.0",
  "description": "Implement a simple chat server supporting some form of simple user identification(authentication would be a nice plus). That means each user should have a unique nickname. Multiple users connected to the server should be able to see each other's messages.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.10.4",
    "jade": "^1.7.0",
    "path": "^0.4.9",
    "prompt": "^0.2.14",
    "socket.io": "^1.2.1",
    "ws": "^0.5.0"
  }
}
